[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-14-workspace"
version = "0.0.1"
description = "Smoke test 14: long results hand off through the run workspace."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["smoke_14_workspace*"]
