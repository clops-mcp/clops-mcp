[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-12-state"
version = "0.0.1"
description = "Smoke test 12: State stores across a composition pipeline."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["smoke_12_state*"]
