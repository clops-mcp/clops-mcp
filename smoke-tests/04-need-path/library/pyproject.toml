[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-04-need-path"
version = "0.0.1"
description = "Smoke test 04: need triggers run failure."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["smoke_04_need_path*"]
