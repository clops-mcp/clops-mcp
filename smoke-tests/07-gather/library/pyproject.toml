[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-07-gather"
version = "0.0.1"
description = "Smoke test 07: gather dispatches N leaf Ops in parallel."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["smoke_07_gather*"]
