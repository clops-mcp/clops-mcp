[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-06-loop"
version = "0.0.1"
description = "Smoke test 06: loop iterates body until predicate."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["smoke_06_loop*"]
