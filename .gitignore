__pycache__/
*.py[cod]
*.egg-info/
*.egg
.pytest_cache/
.coverage
dist/
build/
.venv/
.claude/.clops/

# Agent scratch space — full repo copies, not source.
.claude/worktrees/

# Real secrets for the deployment examples. `.env.example` is the tracked one.
.env
.env.secrets
deploy/**/.env
