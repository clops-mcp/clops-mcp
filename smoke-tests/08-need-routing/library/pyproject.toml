[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-08-need-routing"
version = "0.0.1"
description = "Smoke test 08: need routes to main thread for resolution."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["smoke_08_need_routing*"]
