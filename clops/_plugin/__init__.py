"""Bundled plugin assets (agents/, skills/) shipped with the package.

These are read-only templates copied into a user's project by
`clops init`. The `_` prefix marks the directory as internal —
not part of the public Python API.
"""
