[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-13-gather-subroutine"
version = "0.0.1"
description = "Smoke test 13: a gather branch makes a dynamic sub-Op call."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["smoke_13_gather_subroutine*"]
